---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/App.tsx, src/app/router.tsx, src/app/layout.tsx, src/components/app-sidebar.tsx, src/features/dashboard/index.tsx, src/features/sections/executive-summary/index.tsx, src/features/sections/market-analysis/index.tsx, src/features/sections/product-service/index.tsx, src/features/sections/marketing-strategy/index.tsx, src/features/sections/operations/index.tsx, src/features/sections/financial-projections/index.tsx, src/features/sections/risks-due-diligence/index.tsx, src/features/sections/kpis-metrics/index.tsx, src/features/sections/launch-plan/index.tsx, src/features/scenarios/index.tsx, src/features/export/index.tsx]
autonomous: true
---

<objective>
Build the dashboard shell with sidebar navigation and routing for all 9 business plan sections plus dashboard, scenarios, and export pages.

Purpose: Create the navigable UI skeleton that users will interact with — sidebar navigation drives routing between all sections.
Output: Working dashboard with sidebar that navigates between all 12 pages (dashboard + 9 sections + scenarios + export).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard layout with shadcn/ui Sidebar</name>
  <files>src/components/app-sidebar.tsx, src/app/layout.tsx</files>
  <action>
  1. Install shadcn/ui sidebar component: `npx shadcn@latest add sidebar separator breadcrumb`

  2. Create `src/components/app-sidebar.tsx` — the main sidebar navigation:
     - Use shadcn/ui `Sidebar`, `SidebarContent`, `SidebarGroup`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton` components
     - Navigation groups:
       **Overview:** Dashboard (LayoutDashboard icon)
       **Business Plan:** (9 items, each with icon from lucide-react)
         - Executive Summary (FileText)
         - Market Analysis (TrendingUp)
         - Product & Service (Package)
         - Marketing Strategy (Megaphone)
         - Operations (Settings)
         - Financial Projections (DollarSign)
         - Risks & Due Diligence (ShieldAlert)
         - KPIs & Metrics (BarChart3)
         - Launch Plan (Rocket)
       **Tools:** Scenarios (GitBranch icon), Export (Download icon)
     - Each item links to its route using react-router `Link` component
     - Highlight active route using `useLocation()` from react-router
     - Add sidebar header with "Fun Box" branding text and a jellyfish/wave emoji or icon
     - Add sidebar footer with app version or "Planning Tool" label

  3. Create `src/app/layout.tsx` — the dashboard layout wrapper:
     - Use `SidebarProvider` + `SidebarInset` from shadcn/ui
     - Render `<AppSidebar />` as the sidebar
     - Content area: header bar with `SidebarTrigger` (mobile toggle) + breadcrumb showing current section name
     - Main area: `<Outlet />` from react-router for page content
     - Use Tailwind for spacing: `className="flex-1 p-6"`

  **Do NOT** build a custom sidebar from scratch — use shadcn/ui Sidebar which handles collapse, mobile responsive, and keyboard navigation.
  **Do NOT** put routing logic in the layout — just `<Outlet />`. Router handles page switching.
  </action>
  <verify>npm run build succeeds, no TypeScript errors in sidebar or layout files</verify>
  <done>Sidebar component renders with all 12 navigation items grouped logically, layout wraps content area with header</done>
</task>

<task type="auto">
  <name>Task 2: Create React Router v7 routes with placeholder pages for all sections</name>
  <files>src/app/router.tsx, src/app/App.tsx, src/features/dashboard/index.tsx, src/features/sections/executive-summary/index.tsx, src/features/sections/market-analysis/index.tsx, src/features/sections/product-service/index.tsx, src/features/sections/marketing-strategy/index.tsx, src/features/sections/operations/index.tsx, src/features/sections/financial-projections/index.tsx, src/features/sections/risks-due-diligence/index.tsx, src/features/sections/kpis-metrics/index.tsx, src/features/sections/launch-plan/index.tsx, src/features/scenarios/index.tsx, src/features/export/index.tsx</files>
  <action>
  1. Create placeholder page components for each section. Each is a simple component displaying:
     - Section title as h1
     - Brief description of what will go here
     - Use a `Card` from shadcn/ui to wrap content

     Pages to create (each in its own directory with index.tsx):
     - `src/features/dashboard/index.tsx` — "Dashboard" overview
     - `src/features/sections/executive-summary/index.tsx` — "Executive Summary"
     - `src/features/sections/market-analysis/index.tsx` — "Market Analysis"
     - `src/features/sections/product-service/index.tsx` — "Product & Service"
     - `src/features/sections/marketing-strategy/index.tsx` — "Marketing Strategy"
     - `src/features/sections/operations/index.tsx` — "Operations"
     - `src/features/sections/financial-projections/index.tsx` — "Financial Projections"
     - `src/features/sections/risks-due-diligence/index.tsx` — "Risks & Due Diligence"
     - `src/features/sections/kpis-metrics/index.tsx` — "KPIs & Metrics"
     - `src/features/sections/launch-plan/index.tsx` — "Launch Plan"
     - `src/features/scenarios/index.tsx` — "What-If Scenarios"
     - `src/features/export/index.tsx` — "Export Business Plan"

  2. Create `src/app/router.tsx` with React Router v7 (library mode):
     ```
     Routes structure:
     <Route element={<DashboardLayout />}>
       <Route index element={<Dashboard />} />
       <Route path="executive-summary" element={<ExecutiveSummary />} />
       <Route path="market-analysis" element={<MarketAnalysis />} />
       <Route path="product-service" element={<ProductService />} />
       <Route path="marketing-strategy" element={<MarketingStrategy />} />
       <Route path="operations" element={<Operations />} />
       <Route path="financial-projections" element={<FinancialProjections />} />
       <Route path="risks-due-diligence" element={<RisksDueDiligence />} />
       <Route path="kpis-metrics" element={<KpisMetrics />} />
       <Route path="launch-plan" element={<LaunchPlan />} />
       <Route path="scenarios" element={<Scenarios />} />
       <Route path="export" element={<Export />} />
     </Route>
     ```
     Export an `AppRoutes` component that renders `<Routes>` with all the above.

  3. Update `src/app/App.tsx`:
     - Replace the placeholder content with `<AppRoutes />`
     - The BrowserRouter is already in providers.tsx, so App.tsx just renders routes

  **Use React Router v7 library mode** — import from `react-router`, NOT `react-router-dom`.
  **Do NOT** use lazy loading for now — all pages are tiny placeholders.
  </action>
  <verify>npm run dev works, clicking sidebar items navigates between pages, URL changes reflect correct routes, browser back/forward work</verify>
  <done>All 12 routes work, sidebar navigation switches pages, active route is highlighted in sidebar, no 404s</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm run dev` shows dashboard with working sidebar
- [ ] All 12 sidebar links navigate to correct pages
- [ ] Active route is visually indicated in sidebar
- [ ] Mobile: sidebar collapses/expands via trigger button
- [ ] Browser back/forward navigation works
- [ ] Each page shows its section name
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dashboard layout with sidebar renders correctly
- All 12 routes work and display placeholder content
- Sidebar highlights active route
- No TypeScript errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
