---
phase: 12-integration-and-polish
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: [src/features/sections/operations/index.tsx, src/features/sections/financial-projections/index.tsx, src/features/sections/risks-due-diligence/index.tsx, src/features/sections/kpis-metrics/index.tsx, src/features/sections/launch-plan/index.tsx]
autonomous: true
---

<objective>
Polish section editors part 2: Operations, Financial Projections, Risks, KPIs, Launch Plan.

Purpose: These sections are the most data-heavy (cost breakdowns, financial tables, risk matrices, KPI tracking). They need the Retool/Stripe treatment most — making complex data feel manageable and professional. Tables, charts, and status indicators must be refined.
Output: Five polished section editors with unified data table, chart, and status badge patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-and-polish/12-CONTEXT.md
@.planning/phases/12-integration-and-polish/12-01-PLAN.md

@src/features/sections/operations/index.tsx
@src/features/sections/financial-projections/index.tsx
@src/features/sections/risks-due-diligence/index.tsx
@src/features/sections/kpis-metrics/index.tsx
@src/features/sections/launch-plan/index.tsx
@src/components/page-header.tsx
@src/components/stat-card.tsx
@src/components/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operations and Financial Projections polish</name>
  <files>src/features/sections/operations/index.tsx, src/features/sections/financial-projections/index.tsx</files>
  <action>
  **Operations:**
  - Use `PageHeader` with AI bar
  - **Crew management:** Clean card per crew member with name + hourly rate in a row. Delete button hidden until hover. "Add Crew Member" as outline button.
  - **Capacity section:** Use a clean stat-grid layout for max daily/weekly/monthly booking limits
  - **Equipment list:** Simple card-based list with `card-elevated` items
  - **Cost breakdown:** This is the most complex part. Organize by category:
    - Category headers: `text-sm font-semibold text-muted-foreground uppercase tracking-wide`
    - Cost items: clean grid rows with label left, input right, consistent widths
    - Use `form-grid` (2-column) for cost inputs within each category card
    - Custom expenses: list with add/remove pattern
  - **Cost charts:** Wrap each chart in `card-elevated` with title. Standardize height `h-[240px]`. Use CSS variable colors.
  - **Safety protocols:** Simple checklist-style list items

  **Financial Projections:**
  - Use `PageHeader` with AI bar
  - **Monthly table:** This is the signature data-heavy component.
    - Wrap in `card-elevated` with overflow-x-auto for mobile
    - Table header: `bg-muted/50` background, `text-xs font-medium text-muted-foreground uppercase` headers
    - Table cells: `text-sm tabular-nums` for numbers
    - Input cells: clean, borderless-looking inputs that align with text (`border-0 bg-transparent text-right tabular-nums`)
    - Profit row: subtle background highlight (green-50/50 for positive, red-50/50 for negative)
    - Totals row: `font-semibold` with top border separator
  - **Seasonality presets:** Clean button group for flat/summer-peak
  - **Charts:** Standardize all three charts (area, bar, pie) at `h-[260px]` in `card-elevated` containers with titles
  - **Aggregate stats:** Use `stat-grid` layout with `StatCard` pattern for totals
  - **Unit Economics:** Clean 2x2 or 4-column grid with subtle card treatment

  Do NOT change calculation logic, month generation, or cost summation functions.
  </action>
  <verify>Run `npx tsc --noEmit` and `npm run build`. Verify operations renders with organized cost sections. Verify financial projections table has clean styling.</verify>
  <done>Operations has organized cost breakdown categories and polished charts. Financial Projections has refined monthly table with tabular-nums, standardized charts, and clean aggregate stats. Both use PageHeader and consistent card patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Risks, KPIs, and Launch Plan polish</name>
  <files>src/features/sections/risks-due-diligence/index.tsx, src/features/sections/kpis-metrics/index.tsx, src/features/sections/launch-plan/index.tsx</files>
  <action>
  **Risks & Due Diligence:**
  - Use `PageHeader` with AI bar
  - **Investment verdict banner:** Refine to Stripe-style alert banner
    - Use subtle left border (4px) with verdict color
    - Icon + title + description in a clean row
    - Background: very subtle tint of the verdict color (`bg-green-50/50`, `bg-amber-50/50`, etc.)
  - **Risk cards:** Each risk as a `card-elevated` item
    - Left: colored severity dot (4px circle) + category badge
    - Center: title (`text-sm font-medium`) + description (`text-xs text-muted-foreground`)
    - Mitigation: indented below with subtle border-l
    - Severity badges: clean pills (`text-xs px-2 py-0.5 rounded-full`) with semantic colors
    - Category badges: muted style (`bg-muted text-muted-foreground`)
  - **Compliance checklist:** Clean list with status indicators (circle icons: empty, half, checkmark)
  - **Due diligence checklist:** Similar pattern with priority badges
  - Empty states for all lists using `EmptyState` component

  **KPIs & Metrics:**
  - Use `PageHeader` with AI bar
  - **KPI target cards:** Use `stat-grid` layout
    - Each KPI: `card-elevated` with label, input, and comparison badge
    - Format inputs with proper prefix/suffix styling: currency prefix in muted color, % suffix
    - Comparison badges: cleaner pills — green with checkmark for met, amber with arrow for below
  - **Actuals section:** Cleaner collapsible with smooth animation
  - Overall: data-dense grid that shows all KPIs at a glance

  **Launch Plan:**
  - Use `PageHeader` with AI bar
  - **Stage cards:** Each stage as a `card-elevated` section
    - Stage header: name + date range in a clean row with colored left accent (different color per stage)
    - Tasks: clean list items with status dropdown
    - Status colors: refined pills — `bg-muted` for pending, `bg-blue-100 text-blue-700` for in-progress, `bg-green-100 text-green-700` for done
    - Add task: subtle outline button at bottom of task list
  - **Timeline feel:** Consider a subtle vertical timeline connector between stages (thin line connecting stage cards)
  - Empty state for no stages using `EmptyState`

  Do NOT change data schemas, status enums, or section data logic.
  </action>
  <verify>Run `npx tsc --noEmit` and `npm run build`. Verify all three sections render with polished styling. Verify risk severity badges, KPI comparison badges, and launch status pills display correctly.</verify>
  <done>Risks has refined verdict banner and severity badges. KPIs has clean stat-grid with comparison indicators. Launch Plan has staged cards with timeline feel and status pills. All use PageHeader and consistent patterns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run build` succeeds
- [ ] All five sections use PageHeader
- [ ] Financial table has tabular-nums and clean styling
- [ ] Charts standardized with card-elevated containers
- [ ] Risk severity and category badges consistent
- [ ] KPI cards with comparison indicators
- [ ] Launch stages with timeline and status pills
- [ ] Empty states present where appropriate
- [ ] No data logic changed
</verification>

<success_criteria>

- Data-heavy sections feel organized and professional
- Consistent table, chart, badge, and card patterns
- Financial Projections table is the flagship data-dense component
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-and-polish/12-05-SUMMARY.md`
</output>
