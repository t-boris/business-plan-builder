---
phase: 12-integration-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: [src/features/auth/login-page.tsx, src/features/businesses/index.tsx, src/features/businesses/create-business.tsx]
autonomous: true
---

<objective>
Polish auth and business management screens to professional quality.

Purpose: Login is the first impression. Business list and creation are the first product interactions. These must set the quality bar and feel like a real SaaS product — not a prototype. Stripe-style refinement: clean layout, subtle shadows, professional spacing.
Output: Premium login page, polished business card grid, and refined creation flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-and-polish/12-CONTEXT.md
@.planning/phases/12-integration-and-polish/12-01-PLAN.md

@src/features/auth/login-page.tsx
@src/features/businesses/index.tsx
@src/features/businesses/create-business.tsx
@src/index.css
@src/components/page-header.tsx
@src/components/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Premium login page redesign</name>
  <files>src/features/auth/login-page.tsx</files>
  <action>
  Redesign the login page to feel like a premium SaaS product:

  **Layout:**
  - Full-height centered layout with a subtle gradient or pattern background (use CSS, not images)
  - Suggest: light gray subtle grid or dot pattern on the background, or a very faint radial gradient
  - Login card with the `card-elevated` class from 12-01 for subtle shadow

  **Card Polish:**
  - Card width: `w-full max-w-md` (slightly wider than current max-w-sm for breathing room)
  - Card padding: increase to `p-8` for spacious feel
  - Header: App logo/icon (Briefcase) larger (size-8), centered above title
  - Title: "Welcome back" as `text-xl font-semibold`
  - Subtitle: "Sign in to your business planning workspace" in `text-sm text-muted-foreground`

  **Form fields:**
  - Add labels above inputs: "Email address", "Password" in `text-sm font-medium`
  - Increase input height slightly: `h-10` (shadcn default) is fine but ensure consistent spacing
  - Email input with `type="email"` and `autoComplete="email"`
  - Password input with `autoComplete="current-password"`
  - Space between fields: `space-y-4` (not space-y-3)
  - Sign in button: full width, `h-10`, primary variant

  **Separator:**
  - "or" text between email sign-in and Google button
  - Use: `<div className="relative"><div className="absolute inset-0 flex items-center"><Separator /></div><div className="relative flex justify-center text-xs uppercase"><span className="bg-card px-2 text-muted-foreground">or</span></div></div>`

  **Google button:**
  - Full width, outline variant, `h-10`
  - Google icon + "Continue with Google" text
  - Subtle hover state

  **Error display:**
  - Use a proper alert/banner style: `bg-destructive/10 text-destructive text-sm rounded-md p-3 border border-destructive/20`
  - Place below the sign-in button

  **Footer:**
  - Below the card: "Business Planning Platform" in `text-xs text-muted-foreground` centered

  Do NOT change authentication logic — only visual presentation.
  </action>
  <verify>Run `npx tsc --noEmit` and `npm run build`. Verify login page renders without errors. Verify auth flow still works (no logic changes).</verify>
  <done>Login page has premium SaaS feel: proper spacing, labels, elevated card, separator, background, and refined error states. Auth logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Business list and create flow polish</name>
  <files>src/features/businesses/index.tsx, src/features/businesses/create-business.tsx</files>
  <action>
  Polish the business management screens:

  **Business List Page (`index.tsx`):**

  - Use the new `PageHeader` component from 12-01: title "Your Businesses", description "Manage and access your business plans"
  - Place the "New Business" button in the PageHeader's children slot (right-aligned)

  - **Business cards:** Apply `card-elevated` class for subtle shadow + hover elevation
  - Add a subtle colored left border or top accent strip based on business template type (use template icon color)
  - Business name: `text-base font-semibold` (not too large)
  - Type badge: cleaner pill style with `text-xs font-medium`
  - Description: `text-sm text-muted-foreground line-clamp-2`
  - Footer area: section count + last updated in `text-xs text-muted-foreground`
  - Delete button: ghost variant, only visible on hover (`opacity-0 group-hover:opacity-100 transition-opacity`)

  - **Empty state:** Use new `EmptyState` component from 12-01 with Briefcase icon, "No businesses yet", "Create your first business plan to get started", and action button "Create Business"

  - **Loading state:** Improve skeleton cards to match the polished card layout (skeleton for icon, title, badge, description)

  **Create Business Page (`create-business.tsx`):**

  - Use `PageHeader`: title "Create Business", description "Choose a template and name your business"
  - Back link: Use a subtle ghost button with ArrowLeft icon above the page header

  - **Template picker:**
  - Use a responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`
  - Each template as a `card-elevated` with:
    - Icon in a colored circular background (48px) top-left
    - Template name: `text-sm font-medium`
    - Description: `text-xs text-muted-foreground`
    - Section count: `text-xs text-muted-foreground` at bottom
    - Selected state: `ring-2 ring-primary` border with subtle background
    - Hover: border color change transition

  - **Name/description form:**
  - Clean field layout with labels
  - "Business Name" (required) with `text-sm font-medium` label
  - "Description" (optional) with label and helper text
  - Create button: primary, full width or right-aligned
  - Loading state on create button
  </action>
  <verify>Run `npx tsc --noEmit` and `npm run build`. Verify business list renders with polished cards. Verify create flow works correctly. Verify empty state renders.</verify>
  <done>Business list has elevated cards with hover effects, clean empty state, and polished header. Create flow has refined template picker and form. All visual polish, no logic changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run build` succeeds
- [ ] Login page has professional layout with labels, spacing, and elevated card
- [ ] Business list uses PageHeader and card-elevated
- [ ] Empty state uses EmptyState component
- [ ] Create business has refined template picker
- [ ] No auth or business logic changed
</verification>

<success_criteria>

- Login feels like a premium SaaS product
- Business list and create flow feel cohesive and professional
- New shared components (PageHeader, EmptyState) used successfully
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-and-polish/12-02-SUMMARY.md`
</output>
